# Pando Configuration
#
# This file configures pando's behavior for managing git worktrees.
# See https://github.com/zpyoung/pando for documentation.

# Rsync Configuration
# Controls how files are copied from source tree to new worktrees
[rsync]
enabled = true
flags = [ "--archive", "--exclude", ".git" ]
exclude = [ "dist/", "tsconfig.tsbuildinfo" ]


# Symlink Configuration
# Controls which files are symlinked instead of copied
# Patterns support glob syntax (e.g., "*.json", ".env*")
[symlink]
patterns = [ "CLAUDE.md", ".claude/" ]
relative = true
beforeRsync = true


# Worktree Configuration
# Controls default behavior for worktree operations
[worktree]
# defaultPath = "../worktrees"  # Default parent directory for worktrees
                                # Relative paths resolve from git root
                                # Absolute paths are used as-is
                                # When using --branch without --path, branch name is appended
